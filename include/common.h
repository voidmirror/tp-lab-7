#pragma once

typedef size_t coord_t;

struct Pair
{
	coord_t x;
	coord_t y;
};

const size_t N = 30;
const size_t M = 30;
const size_t ltLimit = 10;
const size_t rtLimit = 5;
const size_t hLimit = 5;